import { defineConfig } from 'astro/config';

export default defineConfig({
  // Replace with your custom domain once set up, e.g. 'https://aaronlow.dev'
  // If using GitHub Pages without a custom domain: 'https://Hawaiianspawn.github.io'
  site: 'https://Hawaiianspawn.github.io',
});
